# Assets

`icon.png` (128×128) is the NuGet package icon, packed into every package via
`src/Directory.Build.props`. The current image is a programmatically generated placeholder
mark; replace it with the official Koras Technologies logo before 1.0 (keep the name and
dimensions — NuGet.org recommends 128×128 PNG, max 1 MB).
