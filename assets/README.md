# Assets

`icon.png` (1254×1254, ~750 KB) is the official Koras logo used as the NuGet package icon,
packed into every package via `src/Directory.Build.props`. Any replacement must stay square
PNG under NuGet.org's 1 MB icon limit (128×128 is the recommended display size).
