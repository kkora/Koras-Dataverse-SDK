# Milestones

> The [master plan §8](master-plan.md#8-delivery-plan) milestone table expanded with
> concrete deliverables and validation per milestone. Sequencing, entry/exit criteria,
> and narrative live in [implementation-plan.md](implementation-plan.md); task detail
> in [backlog.md](backlog.md).
>
> **Status (2026-07-16):** milestones 0–7 substantially complete; `0.1.0-preview.1` and
> `0.1.0-preview.2` published to NuGet.org via Trusted Publishing (milestone 9 largely done —
> `Koras.*` prefix reservation still pending). Milestone 8 (hardening) in progress: PublicAPI
> tracking populated, package-consumption tests (incl. FetchXml netstandard2.0 consumer),
> coverage gate (80/70), and the nightly live-integration workflow are in place; still open:
> a provisioned live test environment (secrets), benchmark baselines, security-checklist dry
> run, and final analyzer severities.

| # | Milestone | Primary features | Deliverables | Validation (how we know it's done) |
|---|---|---|---|---|
| 0 | Repository foundation | — (infrastructure) | Solution + project shells for `src/Koras.Dataverse.Abstractions`, `src/Koras.Dataverse.FetchXml`, `src/Koras.Dataverse`, `src/Koras.Dataverse.OpenTelemetry`, `tests/*`; `Directory.Build.props` + `Directory.Packages.props` (CPM, per-TFM pins); `.editorconfig`; analyzers + PublicAPI tracking (empty txt files); nullable + warnings-as-errors; deterministic build + SourceLink; `EnablePackageValidation`; CI workflows (build/test matrix Linux+Windows × net8/9/10, pack+validate, coverage, CodeQL, dependency review, vulnerable-package scan, Dependabot); README, CONTRIBUTING, CODE_OF_CONDUCT, SECURITY.md, issue/PR templates, CLAUDE.md; package metadata scaffolding (icon, READMEs, tags) | CI green on empty solution across the full matrix; probe violations (analyzer, PublicAPI, dependency rule) each fail CI when introduced deliberately; pack output passes validation; branch protection + `nuget-release` environment shell exist |
| 1 | Abstractions & core models | KDV-002/009/010 (model halves), all interfaces | Complete `Koras.Dataverse.Abstractions`: `Entity`, `EntityReference`, `ColumnSet`, `DataverseQueryResult`, `WhoAmIResponse`, `UpsertResult`; `DataverseError`/`DataverseErrorCategory`/`DataverseException`; `DataverseClientOptions`/`DataverseAuthenticationOptions`/`DataverseRetryOptions` (+ validation attributes); `IDataverseClient`, `IMetadataClient`, `ISolutionClient`, `IDataverseTokenProvider`, `IDataverseClientFactory`; batch/metadata/solution model types; architecture test project | Arch tests prove zero third-party refs, namespace layout, sealed/abstract, Async+`CancellationToken` conventions; model unit tests green; `PublicAPI.Unshipped.txt` matches master plan §4 in review; XML docs on 100 % of public surface |
| 2 | Core implementation | KDV-001, 002, 003, 005, 008, 009 | Token provider (scope `{url}/.default`, 5-min early refresh, single-flight, `TimeProvider`); auth + retry handlers on named client `"Koras.Dataverse:{name}"`; error mapper; CRUD/upsert/alternate keys + `@odata.bind` + POCO mapping; `ODataQuery` builder family + `QueryAsync`/`QueryAllAsync` streaming paging; `$batch` with change sets, 1000-op guard, per-item results; fake `HttpMessageHandler` + fake `TimeProvider` test infra | Test-matrix rows for these features green (happy/invalid/boundary/cancellation/failure/dependency-failure/thread-safety); hostile-input encoding corpus green; redaction tests green; suites run with zero network and zero real-clock delay |
| 3 | DI & configuration | KDV-010 | `AddDataverse` (default + named), `IConfiguration` binding, startup validation (DataAnnotations + HTTPS rule), `IDataverseClientFactory` implementation, singleton lifetimes, full per-name isolation (options/HttpClient/token cache) | KDV-010 matrix rows green; smoke host app boots from documented appsettings shape + user-secrets and executes against fake handler; every misconfiguration case fails at startup with an actionable message |
| 4 | Feature packages | KDV-004, 006, 007 | `Koras.Dataverse.FetchXml` fluent builder (filters, links, ordering, paging cookies, `Top`) via XML writer, netstandard2.0 + net8/9/10, zero deps, own test project; `FetchAsync` + paging-cookie fetch-all in core; documented raw-FetchXML escape hatch with injection warning; `IMetadataClient` (tables, columns, local/global choices, relationships); `ISolutionClient` (export, import + async job polling, publish-all, installed query) | KDV-004/006/007 matrix rows green; FetchXml ns2.0 asset builds and its escaping corpus passes; polling cancelable and `TimeProvider`-driven (no sleeps in tests); arch test still shows FetchXml zero-dep |
| 5 | ASP.NET Core integration | KDV-012 | `AddDataverseHealthCheck()` WhoAmI probe with documented healthy/degraded/unhealthy mapping and secret-free descriptions; minimal-API wiring exercise (seed of the M7 sample) | KDV-012 matrix rows green; `/health` endpoint behaves per mapping against scripted fake responses (success/401/429/network failure) |
| 6 | Observability | KDV-011 | `ActivitySource "Koras.Dataverse"` activities wrapping retries; `Meter` counters/histograms; structured logging with stable categories/properties per [data-protection policy](../security/data-protection.md); zero-cost no-listener path; `Koras.Dataverse.OpenTelemetry` package (`TracerProviderBuilder`/`MeterProviderBuilder` extensions, OpenTelemetry.Api only) | KDV-011 matrix rows green; tag/log policy tests (no row data, no tokens) green; arch test: core has no OTel reference; OTel package passes consumption test |
| 7 | Samples & documentation | all MVP (adoption surface) | `samples/` Console, MinimalApi, Worker building against packed bits; five-minute quick start; recipes (throttling, paging, batch, metadata); ServiceClient comparison guide; per-feature docs KDV-001..012; XML-docs completeness sweep | Samples restore+build+run in CI from local feed; quick start executed cold by a non-author in ≤ 5 minutes; docs review sign-off; master plan §3 "ships with" list satisfied per feature |
| 8 | Hardening | quality/security/perf gates | Live integration suite (env-var gated, unique-prefix isolation, sweeper) + nightly workflow; threat-model review vs. implemented code; security-checklist dry run; benchmark suites per [benchmarks.md](../performance/benchmarks.md) with archived baseline; coverage gate ≥ 80 % line / ≥ 70 % branch enforced; analyzers at final severity; parser corpus expansion | Nightly live run green against real environment; dry-run findings closed or tracked with owners; baseline artifacts archived; coverage gate on and passing; zero unjustified suppressions |
| 9 | Packaging & release | 0.1.0-preview.1 | Tag-triggered release workflow (verify → test → live gate → benchmarks → pack → sign-when-configured → gated publish → GitHub Release with notes/symbols/SBOMs); `nuget-release` environment + `NUGET_API_KEY`; NuGet.org organization setup; local-feed dry run; CHANGELOG; executed release + security checklists; `Koras.*` prefix reservation filed | `0.1.0-preview.1` installable from NuGet.org into a clean project; listing renders (README/icon/MIT/deps per TFM); symbols + SourceLink round trip; checklists archived in the release issue |

## Post-MVP milestones (release plan, indicative)

Per master plan §8's release plan; to be broken down when their train opens:

| Release | Contents (from master plan §3) |
|---|---|
| 0.1.0 | Stabilized MVP after the preview feedback window; API adjustments from feedback |
| 0.5.0 | KDV-013 impersonation, KDV-014 file/image columns, KDV-015 OrganizationService package (each requires a threat-model update before implementation) |
| 1.0.0 | API freeze: PublicAPI promotion, package-validation baseline, author signing, LTS policy active |
